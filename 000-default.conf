Define KEYCLOAK_ADDR http://key-cloak.dev.cloudapps.ao.dcn
Define KEYCLOAK_REALM AOUSCourts
Define CLIENT_ID php-mao-test
Define CLIENT_SECRET 5bf751b6-8207-4c7c-9992-42a3010f41ea
Define HOST_IP mao-mod-auth-openidc.dev.cloudapps.ao.dcn

#this is required by mod_oidc
OIDCCryptoPassphrase currently-not-supported-by-keycloak

OIDCProviderMetadataURL ${KEYCLOAK_ADDR}/auth/realms/${KEYCLOAK_REALM}/.well-known/openid-configuration

OIDCClientID ${CLIENT_ID}
OIDCClientSecret ${CLIENT_SECRET}
OIDCRedirectURI http://${HOST_IP}/protected/redirect_uri

OIDCRemoteUserClaim preferred_username

<Location /protected/>
    AuthType openid-connect
    Require valid-user
</Location>

